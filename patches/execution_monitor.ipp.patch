diff --git a/include/boost/test/impl/execution_monitor.ipp b/include/boost/test/impl/execution_monitor.ipp
index 848670b8..c513d668 100644
--- a/include/boost/test/impl/execution_monitor.ipp
+++ b/include/boost/test/impl/execution_monitor.ipp
@@ -400,6 +400,7 @@ system_signal_exception::report() const
         return; // no error actually occur?
 
     switch( m_sig_info->si_code ) {
+#if 0
 #ifdef __VXWORKS__
 // a bit of a hack to adapt code to small m_sig_info VxWorks uses
 #define si_addr si_value.sival_int
@@ -410,15 +411,18 @@ system_signal_exception::report() const
                       "signal: generated by kill() (or family); uid=%d; pid=%d",
                       (int)m_sig_info->si_uid, (int)m_sig_info->si_pid );
         break;
+#endif
 #endif
     case SI_QUEUE:
         report_error( execution_exception::system_error,
                       "signal: sent by sigqueue()" );
         break;
+#if 0
     case SI_TIMER:
         report_error( execution_exception::system_error,
                       "signal: the expiration of a timer set by timer_settimer()" );
         break;
+#endif
 // OpenBSD was missing SI_ASYNCIO and SI_MESGQ
 #ifdef SI_ASYNCIO
     case SI_ASYNCIO:
